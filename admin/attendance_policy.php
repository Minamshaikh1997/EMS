<?php
require_once __DIR__ . '/../config/back_dashboard_register.php';
session_start();include('admincheck_role.php');include('../config/db.php');include('../config/attendance.php');
$message='';if($_SERVER['REQUEST_METHOD']==='POST'&&verifyAttendanceCsrf()){$v=[];foreach(['grace_minutes','half_day_minutes','full_day_minutes','overtime_after_minutes','allow_early_check_in_minutes'] as $k)$v[$k]=max(0,(int)($_POST[$k]??0));$admin=(int)($_SESSION['admin_id']??0);$s=$conn->prepare('UPDATE attendance_policy SET grace_minutes=?,half_day_minutes=?,full_day_minutes=?,overtime_after_minutes=?,allow_early_check_in_minutes=?,updated_by=? WHERE id=1');$s->bind_param('iiiiii',$v['grace_minutes'],$v['half_day_minutes'],$v['full_day_minutes'],$v['overtime_after_minutes'],$v['allow_early_check_in_minutes'],$admin);$s->execute();$s->close();$message='Attendance policy updated.';}$p=attendancePolicy($conn);$csrf=attendanceCsrfToken();
?><!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Attendance Policy</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"></head><body class="bg-light"><main class="container py-5"><div class="card shadow-sm"><div class="card-body p-4"><div class="d-flex justify-content-between"><h2>Attendance Policy</h2><a href="attendance_report.php" class="btn btn-outline-secondary">Attendance Report</a></div><?php if($message):?><div class="alert alert-success"><?=$message?></div><?php endif;?><form method="post" class="row g-3"><input type="hidden" name="csrf_token" value="<?=htmlspecialchars($csrf)?>"><?php foreach(['grace_minutes'=>'Late grace minutes','half_day_minutes'=>'Half-day minimum minutes','full_day_minutes'=>'Full-day minimum minutes','overtime_after_minutes'=>'Overtime starts after minutes','allow_early_check_in_minutes'=>'Early check-in window minutes'] as $k=>$label):?><div class="col-md-4"><label class="form-label fw-bold"><?=$label?></label><input type="number" min="0" max="1440" name="<?=$k?>" value="<?=(int)$p[$k]?>" class="form-control" required></div><?php endforeach;?><div class="col-12"><button class="btn btn-primary">Save Policy</button></div></form></div></div></main></body></html>
